# PnBook - Frontend (pnbook-fe)
The frontend project for the PnBook system, is built with React 19, Vite and TypeScript.

## Tech Stack
- **Core:** React 19, TypeScript, Vite
- **Styling:** TailwindCSS v4, clsx, tailwind-merge
- **State Management:** React Query (TanStack Query)
- **Forms:** React Hook Form
- **Code Quality:** ESLint (Flat Config), Prettier, Husky, Lint-staged, Commitlint

## Setup requirement
- Node.js >= 18
- Package Manager: npm/pnpm/yarn

## Install & run the project
1. **Clone project:**
   ```
   git clone https://github.com/TaiVo1905/pnbook-fe-repo.git
   cd pnbook-fe
   ```
2. **Install dependencies**

   ```
   npm install
   ```
3. **Start project**
   ```
   npm run dev
   ```

## Branch name and mommit discription
- **Branch name:** type/JIRA-KEY-discription (Example: feat/PNBOOK-1-login)
- **Commit discription:** type(JIRA-KEY): discription (Example: feat(PNBOOK-1): add button component)
- **Type:** `feat`, `fix`, `chore`, `style`, `refactor`, `test`...
- **JIRA-KEY example:** `PNBOOK-1`